# 安全政策

本政策适用于 **校园网无感认证 · CampusNetAuth**（湖北工业大学 iHBUT 校园网自动认证工具）。

## 支持范围

- 仅支持 **最新 Release**（当前：`V0.1`）。
- 旧版本可能存在已知或未知问题，请优先升级到最新版后再反馈。
- 源码状态以 `main` 分支为准；未发布分支的代码不属于支持范围。

## 漏洞 / 安全问题上报

发现安全问题（而非普通 bug）请通过以下渠道反馈：

- **GitHub Issues**：<https://github.com/Origin-XiChen/campus-net-auth/issues>
- **Private vulnerability reporting**：仓库页面 → `Security` → `Report a vulnerability`（如已启用）。

反馈时请尽量包含：

1. 影响版本（Release 号或 commit 号）
2. 复现步骤与现象描述
3. 影响面评估（可读到什么 / 能造成什么后果）
4. （如涉及凭据）**切勿**在公开渠道贴出真实账号密码或 `cred.bin` 内容

个人项目的维护节奏有限，收到后会尽快响应并评估修复。

## 安全设计

本工具在设计与实现上遵循以下原则：

| 方面 | 说明 |
|---|---|
| 凭据存储 | 密码使用 Windows **DPAPI**（`CryptProtectData`）加密，加密绑定当前 Windows 用户账户；`cred.bin` 被拷到其它机器/其它用户下无法解密。 |
| 网络范围 | 所有请求走校园网内网 HTTP，凭据**只发送给门户服务器** `172.16.54.18`，不与任何第三方服务通信。 |
| 明文泄露 | 任何界面与日志均不打印明文密码；日志中的账号默认做脱敏处理。 |
| 行为边界 | 程序只做「自己账号的自动登录」，不做绕过计费、共享账号或其它越权行为。 |
| 传输加密 | 门户为校园网内网 HTTP 门户（设备侧控制，不支持 HTTPS）；请确保你在可信的校园网内使用本工具。 |

## 数据文件保护

运行时会生成以下含敏感信息的文件（默认与 exe 同目录）：

- `config.json` —— 含账号等配置
- `cred.bin` —— DPAPI 加密后的凭据
- `state.json` / `*.log` —— 运行状态与日志（日志已脱敏）

这些文件**均已加入 `.gitignore`，不会进入版本库**。请勿将其打包进任何分发物或上传到公开渠道；分享/备份时请手动排除它们。

## 使用建议

- 仅在受信任的 Windows 账户下运行；不要把 `cred.bin` 与 `config.json` 外传。
- 如怀疑凭据泄露，重新运行 `CampusNetAuth.exe setup` 修改密码即可覆盖旧凭据。
- 校园网认证页面若长期无法访问，请以学校网络中心公告为准。
